package smokel

/*
func test() {

	ms := msg.New()

	lx := linexer.New(ms)
	//dr := lx.Linexer("uia", strings.NewReader("//comment"))

	parser(lx, ms)

}

func compiler(
	msg,
	linexer,
	parser,
)

func parser(lnx linexer.Reader, msg msg.Writer) {
	lines := lnx.Linexer("myMockFile", strings.NewReader("//comment"))

	for _, line := range lines {

		tp := linexer.NewTypeReader(line)

		linexer.

		if tp.IsComment() {
			fmt.Println("Encontrei um comment!", line.Number, line.Name)
		}

	}

	fmt.Println(lines)

}

*/
