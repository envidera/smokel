package data

/*
type Adapter struct{}

func (t *Adapter) SliceLinexerLineToParserLine(lines []LinexerLine) []ParserLine {

	adp := []ParserLine{}

	for _, ln := range lines {
		adp = append(adp, t.LinexerLineToParserLine(ln))
	}

	return adp
}

func (t *Adapter) LinexerLineToParserLine(line LinexerLine) ParserLine {

	adp := ParserLine{
		Number:    line.Number,
		Name:      line.Name,
		Type:      int(line.Type),
		Formatted: line.Formatted,
	}

	return adp
}
*/
